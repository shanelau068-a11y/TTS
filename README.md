# 声场 TTS 工作台

纯前端文字转语音网页，可直接部署到 GitHub Pages。

- 浏览器内置语音免费试听（无需密钥）
- 支持兼容 OpenAI `POST /v1/audio/speech` 的 API：填写地址、密钥、模型、音色和语速即可导出
- 支持 `角色：台词`、`【角色】台词`、`角色 - 台词` 的广播剧文本
- 长文按自然句拆分并以交叉淡化拼接为单个 WAV 文件

密钥只由浏览器直接发往你填写的 API，不会被本网站保存或转发。

推送后请在仓库 **Settings → Pages** 中将 Source 设为 **GitHub Actions**。
